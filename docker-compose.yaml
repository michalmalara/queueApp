services:
  web:
    build: .
    ports:
      - "8080:8080"
    command: python manage.py runserver 0.0.0.0:8080
    volumes:
      - .:/code
    env_file:
      - secrets.env
    environment:
      - DEBUG=True